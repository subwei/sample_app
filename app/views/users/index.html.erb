<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%# Renders each user with the _user partial by convention %>
  <%= render @users %>
</ul>

<%# Don't need to specify will_paginate @users b/c rails assumes this from the controller %> 
<%= will_paginate %>